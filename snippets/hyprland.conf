# Add to ~/.config/hypr/hyprland.conf
# Applies the per-application overrides in ~/.config/keyd/app.conf.
# Relies on window-title detection and is unreliable under Hyprland --
# the terminal-side snippet is the load-bearing fix, this is a bonus.
exec-once = keyd-application-mapper -d
