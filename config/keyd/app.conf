# Per-application overrides, applied on top of /etc/keyd/default.conf
# by keyd-application-mapper.

# Terminal: keep the real Ctrl+W (neovim window prefix; readline word-delete)
# instead of the global Ctrl+W -> Ctrl+Backspace remap.
[Alacritty]

control.w = C-w
